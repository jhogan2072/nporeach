<% @page_title = t('.creatediscount') %>

<%= form_for([:admin, @subscription_discount], :html => {:class => "standard_form", :multipart => true}) do |form| %>
  <%= render :partial => 'form', :object => form %>
<% end %>
